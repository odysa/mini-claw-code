// Re-export from types — Tool trait and ToolSet live there
// Tool implementations will be added in Phase 2 (Ch 6-9)
pub use crate::types::{Tool, ToolDefinition, ToolResult, ToolSet, ValidationResult};
